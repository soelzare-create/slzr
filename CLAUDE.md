# CLAUDE.md — نسخه فعلی (فاز ۱) — نرم‌افزار حسابداری و فاکتور داران ایکس

این فایل راهنمای نسخه فعلی پروژه برای Claude Code است. قبل از هر کاری کامل بخوان.
زبان رابط کاربری: فارسی (راست‌به‌چپ / RTL). زبان کد و کامنت: انگلیسی. این فایل،
دامنه‌ی فاز فعلی را تعریف می‌کند: **بدون ماژول انبار**. فلسفه داران ایکس: «همه چیز سر
جای خودش» — روش: شناخت ← طراحی ← اجرا ← پشتیبانی.

## ۱. هدف پروژه (فاز فعلی)

نرم‌افزار یکپارچه حسابداری و فاکتور برای شرکت داران ایکس.

- حسابداری کامل: دفتر کل، اسناد، ترازنامه.
- فاکتور و فرآیند فروش: سه نوع فاکتور با جریان‌های مجزا.
- کاربران هم‌زمان: ۶ تا ۲۰ نفر.
- اصل یکپارچگی: اسناد مالی به‌صورت خودکار از رویدادهای فاکتور و خرید تولید شوند، نه دستی.
- تفاوت کلیدی این فاز: ماژول انبار ساخته نمی‌شود (بخش ۷).

## ۲. استک فنی

- بک‌اند: **Django + Django REST Framework** (پایتون) — ORM قوی، تراکنش اتمیک.
- دیتابیس: **PostgreSQL** (پیش‌فرض توسعه: SQLite بدون نیاز به راه‌اندازی).
- فرانت‌اند: **React (Vite)** + پشتیبانی کامل RTL.
- احراز هویت: نقش‌محور (RBAC) با ماتریس مجوز دیتابیسی (نه سخت‌کد) + JWT.
- معماری: **مونولیت ماژولار** — یک پروژه Django با اپ‌های تفکیک‌شده هم‌راستا با بخش‌ها.

اپ‌های Django (فاز فعلی): `core`، `accounts`، `procurement`، `sales`، `accounting`،
`technical`، `management`. هر اپ مالک مدل‌های خودش است؛ منطق کسب‌وکار در لایه
`services.py`، نه در view/model.

### قواعد کد
- هر عملیات چندجدولی داخل `transaction.atomic()`. فاکتور هرگز نصفه ثبت نشود.
- هر تغییر وضعیت در `AuditLog` ثبت شود.
- رقابت روی منبع مشترک → قفل ردیف (`select_for_update()`).
- اعلانات خارج از مسیر بحرانی: ثبت فاکتور نباید به داشبورد اعلانات وابسته باشد.

## ۳. بخش‌ها و ماتریس دسترسی (RBAC مجوز-محور دیتابیسی)

شش بخش. هر بخش (جز مدیریت) دو نقش دارد: مدیر و کارمند.

- `System` — بخش‌ها. `Permission` — مجوز اتمیک (`sales.view`, `sales.edit`, `sales.view_all`).
- `Role` — دو نقش هر بخش + نقش مدیریت + ادمین سیستم. `RolePermission` — قلب ماتریس.
- `UserRole` — اتصال کاربر به نقش. «کارمند فقط کار خودش» در سطح object-level (فیلتر
  queryset با `owner=user`)، نه سطح نقش.
- ادمین سیستم: جدا از بخش مدیریت (پرچم `is_system_admin`). مدیریت: دسترسی کامل همه بخش‌ها.
- حسابداری (بین‌بخشی): همه فاکتورها فقط خواندنی.

مزیت: افزودن بخش یا تغییر دسترسی = یک ردیف در `RolePermission`، بدون تغییر کد.

## ۴. انواع فاکتور
- **۴.۱ فروش کالا** (کامل‌ترین) — بخش ۵.
- **۴.۲ خدمات** — خدمت + قیمت، بدون انبار.
- **۴.۳ پشتیبانی ماهانه** — مانند خدمات + بازه زمانی (`period_start/period_end`)؛ تمدید
  خودکار نیست، فقط یادآوری در داشبورد اعلانات.

> **به‌روزرسانی جریان (نسخهٔ فعلی):** خرید و انبار از پیش‌فاکتور/فاکتور **جدا** شده‌اند.
> پیش‌فاکتور بدون مرحلهٔ «تأیید/رزرو» مستقیم به فاکتور تبدیل می‌شود؛ همان لحظه درخواست
> خرید برای بازرگانی صادر می‌شود. هر خرید مستقیماً به **فاکتور** پیوند می‌خورد (یک فاکتور
> می‌تواند چند خرید از تأمین‌کننده‌های مختلف داشته باشد). قانون ۵٪ و پیش‌نیازِ خریدِ قبل از
> فاکتور حذف شده‌اند. فاکتور فروش فقط «دریافتنی/درآمد» می‌زند و خرید هنگام ثبت،
> «بهای‌تمام‌شده/پرداختنی» را ثبت می‌کند. سریال‌های واردشده در انبار از طریق پیوندِ
> خرید↔فاکتور به فاکتور تخصیص می‌یابند. بخش‌های ۵ و ۶ زیر تاریخی‌اند.

## ۵. جریان فاکتور فروش کالا (state machine)

`DRAFT → CONFIRMED → AWAITING_PURCHASE → READY → INVOICED`
جانبی: `CANCELLED`، `UNFULFILLABLE`.

- **CONFIRMED:** رزرو نرم ۴۸ ساعته (job زمان‌بندی‌شده آزاد می‌کند). ابطال تأییدشده فقط با
  تأیید مدیر. رقابت رزرو: FCFS؛ در هم‌زمانی واقعی، شکستن تساوی = حاشیه سود بالاتر.
- **AWAITING_PURCHASE → READY:** بازرگانی خرید را ثبت می‌کند؛ هر قلم فروش به خرید مشخص
  خودش وصل (یک‌به‌یک). ثبت خرید → سند حسابداری (بدهی به تأمین‌کننده) + اعلان «آماده فاکتور».
- **INVOICED:** «تبدیل به فاکتور» + کنترل قاعده ۵٪ روی هر قلم + سند فروش خودکار.
- مسیرهای بازگشت: ابطال/مرجوعی/ابطال خرید → سند مالی معکوس. همه در `AuditLog`.

## ۶. قوانین کسب‌وکار
- **قانون ۵٪:** قیمت فروش هر قلم ≥ قیمت خرید همان قلم × ۱٫۰۵. روی تک‌تک اقلام،
  غیرقابل دور زدن هنگام ثبت فاکتور.
- مبنای قیمت خرید: هر قلم فروش به خرید مشخص خودش وصل (بدون میانگین/FIFO).
- پیش‌نیاز فروش: قبل از فاکتور، خرید باید ثبت شده باشد.
- رزرو نرم ۴۸ ساعته: `python manage.py release_reservations` (cron/Celery beat).

## ۷. وضعیت ماژول انبار
در فاز فعلی ساخته نمی‌شود. نقش «تأیید آماده‌بودن کالا» را ثبت خرید توسط بازرگانی ایفا
می‌کند. مدل `Item` به‌عنوان پایه آینده نگه داشته شده؛ `SerialUnit/Warehouse/StockTransaction`
ساخته نمی‌شوند.

## ۸. مدل داده (فاز فعلی)
`User` · `System/Permission/Role/RolePermission/UserRole` · `Party` · `Item` ·
`Purchase/PurchaseLine` · `Proforma/ProformaLine` · `Invoice/InvoiceLine` ·
`Account` · `JournalEntry/JournalLine` · `Notification` · `AuditLog`.

اتصال یکپارچگی: ثبت خرید → JournalEntry (خرید/بدهی) + اعلان؛ ثبت فاکتور فروش →
JournalEntry (فروش، شامل بهای تمام‌شده)؛ ابطال/مرجوعی → JournalEntry معکوس.

## ۹. تصمیم‌های نهایی‌شده (پیاده‌سازی‌شده)
قانون ۵٪ روی تک‌تک اقلام · هر قلم فروش به خرید یک‌به‌یک · رزرو نرم ۴۸ ساعت + آزادسازی
خودکار · ابطال تأییدشده فقط با مدیر · FCFS + تساوی=حاشیه سود · دسترسی مدیر بخش کامل ·
ادمین سیستم جدا · حسابداری بین‌بخشی فقط‌خواندنی · فروش↔بازرگانی دوطرفه · پشتیبانی
ماهانه فقط یادآوری · اعلان آماده فاکتور به‌محض ثبت خرید · انبار غیرفعال · RBAC دیتابیسی.

## ۱۰. کدینگ حساب‌ها و اثر مالی (تصمیم‌های حل‌شده)
کدینگ در `apps/management/management/commands/seed.py`:
دارایی‌ها (۱۰۰۰): صندوق/بانک ۱۱۰۰، دریافتنی ۱۲۰۰، موجودی کالا ۱۳۰۰ · بدهی‌ها (۲۰۰۰):
پرداختنی ۲۱۰۰ · سرمایه ۳۰۰۰ · درآمدها (۴۰۰۰): فروش ۴۱۰۰، خدمات ۴۲۰۰، پشتیبانی ۴۳۰۰ ·
هزینه‌ها (۵۰۰۰): بهای تمام‌شده ۵۱۰۰.

- ثبت خرید: موجودی کالا (بد) / پرداختنی به تأمین‌کننده (بس).
- فاکتور فروش کالا: دریافتنی (بد) / درآمد فروش (بس) + بهای تمام‌شده (بد) / موجودی (بس).
- فاکتور خدمات/پشتیبانی: دریافتنی (بد) / درآمد خدمات یا پشتیبانی (بس).

## ۱۱. اجرا و تست
راهنمای راه‌اندازی در `README.md`. تست‌ها: `cd backend && pytest -q`.
