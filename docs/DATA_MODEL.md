# نقشه داده داران‌ایکس

هر جدول یک نوع اطلاعات را نگه می‌دارد. دو نکته کلیدی بلوپرینت:

1. کالاها دو نوع‌اند: **سریال‌دار** (`unit_items`) و **مقداری/فله‌ای**
   (`inventory_movements`).
2. هر **فاکتور** صادرکننده مشخص دارد (`invoices.issuer_id`) برای محاسبه پورسانت.

---

### `users` — کاربران (تیم شرکت)

| فیلد               | نوع    | توضیح                                       |
| ------------------ | ------ | ------------------------------------------- |
| id                 | PK     | شناسه                                        |
| name               | متن    | نام                                          |
| role               | enum   | مدیر/فروش/فنی/انباردار/حسابدار               |
| password_hash      | متن    | رمز ورود (bcrypt)                            |
| phone              | متن    | شماره تماس (شناسه ورود، یکتا)                |
| address            | متن    | آدرس                                         |
| emergency_contact  | متن    | تماس اضطراری                                 |
| is_active          | بولی   | فعال/غیرفعال (حذف نرم)                        |

### `parties` — طرف‌حساب‌ها (مشتری و/یا تأمین‌کننده)

یک رکورد واحد برای هر طرفی که با او کار می‌کنیم. می‌تواند مشتری باشد، تأمین‌کننده،
یا **هر دو** — بدون تکرار. مانده‌ی خالص هر طرف از همین‌جا محاسبه می‌شود.

| فیلد            | نوع   | توضیح                              |
| --------------- | ----- | ---------------------------------- |
| id              | PK    | شناسه                               |
| name            | متن   | نام شخص یا سازمان                    |
| phone           | متن   | شماره تماس                          |
| address         | متن   | نشانی                               |
| is_customer     | بولی  | نقش مشتری دارد                      |
| is_supplier     | بولی  | نقش تأمین‌کننده دارد                |
| contact_date    | تاریخ  | تاریخ آشنایی (برای نقش مشتری)       |
| referral_source | متن   | مدل آشنایی (برای نقش مشتری)         |

> قانون: هر طرف‌حساب باید حداقل یک نقش داشته باشد. جدول‌های `activities` و
> `tickets` با `customer_id` به همین جدول (نقش مشتری) وصل می‌شوند و `purchases`
> با `supplier_id` (نقش تأمین‌کننده).

### `activities` — فعالیت‌ها (جدول مرکزی)

| فیلد        | نوع  | توضیح                              |
| ----------- | ---- | ---------------------------------- |
| id          | PK   | شناسه                               |
| customer_id | FK   | مشتری                               |
| owner_id    | FK   | مسئول (کاربر)                        |
| type        | enum | پروژه/فروش/قرارداد پشتیبانی          |
| status      | enum | باز/در حال انجام/انجام‌شده/لغوشده    |

### `project_stages` — مراحل پروژه (فقط پروژه‌ها)

| فیلد        | نوع   | توضیح                                  |
| ----------- | ----- | -------------------------------------- |
| id          | PK    | شناسه                                   |
| activity_id | FK    | فعالیت (پروژه)                          |
| stage       | enum  | شناخت/طراحی/پیش‌فروش/اجرا/پشتیبانی      |
| entered_at  | تاریخ  | تاریخ ورود به مرحله                     |

### `product_models` — مدل کالا

| فیلد          | نوع   | توضیح                         |
| ------------- | ----- | ----------------------------- |
| id            | PK    | شناسه                          |
| name          | متن   | نام مدل                        |
| tracking_type | enum  | سریال‌دار/مقداری               |
| base_price    | عدد   | قیمت پایه                      |
| specs         | متن   | مشخصات فنی                     |

### `unit_items` — تک‌کالا (سریال‌دار)

| فیلد          | نوع   | توضیح                              |
| ------------- | ----- | ---------------------------------- |
| id            | PK    | شناسه                               |
| model_id      | FK    | مدل                                 |
| serial_number | متن   | شماره سریال یکتا                    |
| status        | enum  | انبار/فروخته/نصب‌شده/خراب           |

### `inventory_movements` — حرکت انبار (مقداری)

| فیلد      | نوع   | توضیح          |
| --------- | ----- | -------------- |
| id        | PK    | شناسه           |
| model_id  | FK    | مدل             |
| quantity  | عدد   | متراژ/تعداد     |
| direction | enum  | ورود/خروج       |

### `activity_items` — اقلام فعالیت

| فیلد             | نوع  | توضیح                                   |
| ---------------- | ---- | --------------------------------------- |
| id               | PK   | شناسه                                    |
| activity_id      | FK   | فعالیت                                   |
| unit_item_id     | FK?  | کالای سریال‌دار (یا)                     |
| product_model_id | FK?  | مدل کالای فله‌ای                         |
| quantity         | عدد? | مقدار (برای فله‌ای)                      |
| price            | عدد  | قیمت این قلم                             |

### `invoices` — فاکتورها

| فیلد         | نوع  | توضیح                          |
| ------------ | ---- | ------------------------------ |
| id           | PK   | شناسه                           |
| activity_id  | FK   | فعالیت                          |
| issuer_id    | FK   | صادرکننده (برای پورسانت)         |
| total_amount | عدد  | مبلغ کل                         |
| status       | enum | پرداخت‌شده/معوق/پرداخت‌نشده      |

### `purchases` — خرید (سند خرید از تأمین‌کننده)

قرینهٔ `invoices` در سمت خرید.

| فیلد         | نوع  | توضیح                          |
| ------------ | ---- | ------------------------------ |
| id           | PK   | شناسه                           |
| supplier_id  | FK   | تأمین‌کننده (طرف‌حساب)           |
| buyer_id     | FK   | کاربری که خرید را ثبت کرده       |
| reference    | متن  | شماره سند/فاکتور فروشنده         |
| total_amount | عدد  | مبلغ کل                         |
| status       | enum | پرداخت‌شده/معوق/پرداخت‌نشده      |

### `purchase_items` — اقلام خرید

قرینهٔ `activity_items`.

| فیلد             | نوع  | توضیح                                   |
| ---------------- | ---- | --------------------------------------- |
| id               | PK   | شناسه                                    |
| purchase_id      | FK   | خرید                                    |
| unit_item_id     | FK?  | کالای سریال‌دار دریافتی (یا)            |
| product_model_id | FK?  | مدل کالای فله‌ای                         |
| quantity         | عدد? | مقدار (برای فله‌ای)                      |
| unit_cost        | عدد  | بهای تمام‌شدهٔ این قلم                   |

### `financial_documents` — اسناد مالی

| فیلد        | نوع  | توضیح                          |
| ----------- | ---- | ------------------------------ |
| id          | PK   | شناسه                           |
| invoice_id  | FK?  | فاکتور فروش مرتبط (برای درآمد)  |
| purchase_id | FK?  | سند خرید مرتبط (برای هزینه)     |
| party_id    | FK?  | طرف‌حساب (برای محاسبهٔ مانده)   |
| amount      | عدد  | مبلغ                            |
| type        | enum | دخل/خرج                         |

### `tickets` — تیکت‌ها (پشتیبانی)

| فیلد           | نوع  | توضیح                          |
| -------------- | ---- | ------------------------------ |
| id             | PK   | شناسه                           |
| customer_id    | FK   | مشتری                           |
| device_unit_id | FK?  | سریال دستگاه (تک‌کالا)          |
| owner_id       | FK?  | مسئول پیگیری                     |
| status         | enum | باز/در حال بررسی/بسته            |

---

## جریان نمونه «فروش یک کالا»

```
فروشنده کالا را انتخاب می‌کند
   → فاکتور ساخته می‌شود            (invoices)
      → پیام به انبار: «یکی کم کن»   (unit_items.status = sold / inventory_movements out)
      → پیام به حسابداری: «ثبت کن»    (financial_documents type=income)
```

هر بخش فقط کار خودش را انجام می‌دهد؛ ارتباط از راه پیام است، نه دست‌بردن مستقیم.

## جریان نمونه «خرید یک کالا» (قرینهٔ فروش)

```
انباردار تأمین‌کننده و کالا را انتخاب می‌کند
   → سند خرید ساخته می‌شود           (purchases)
      → پیام به انبار: «یکی اضافه کن»  (unit_items جدید / inventory_movements ورود)
      → پیام به حسابداری: «خرج ثبت کن»  (financial_documents type=expense, party_id)
```

## مانده‌ی هر طرف‌حساب

`مانده = مجموع دخل (فروش به او) − مجموع خرج (خرید از او)` — چون هر دو سمت به یک
رکورد `parties` وصل‌اند، یک عدد واحد برای هر طرف به دست می‌آید.
